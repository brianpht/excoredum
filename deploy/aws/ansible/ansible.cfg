[defaults]
inventory = hosts.ini
host_key_checking = False
remote_user = ec2-user
private_key_file = ~/.ssh/excoredum-bench
interpreter_python = auto_silent
retry_files_enabled = False
timeout = 60

[ssh_connection]
pipelining = True
